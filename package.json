{
  "name": "dl-dvt",
  "version": "1.0.0",
  "description": "DenimLabs Discord Voicenote Transcriber (DL-DVT).",
  "private": true,
  "scripts": {
    "start": "/usr/bin/electron app/main.js",	
    "start:helper": "npm --prefix helper start",
    "dev": "/usr/bin/electron app/main.js",
    "install:helper": "npm --prefix helper install"
  },
  "devDependencies": {
    "electron": "^39.8.10"
  }
}
